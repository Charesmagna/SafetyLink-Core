name: Build Windows EXE
on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 22

      - name: Compute version
        id: version
        shell: bash
        run: |
          VERSION="1.1.${{ github.run_number }}"
          echo "tag=v${VERSION}" >> $GITHUB_OUTPUT
          echo "version=${VERSION}" >> $GITHUB_OUTPUT

      - name: Install dependencies (Windows — regenerate lockfile for native bindings)
        shell: bash
        run: |
          rm -f package-lock.json
          npm install --prefer-offline

      - name: Build Web
        shell: bash
        env:
          VITE_APP_VERSION: ${{ steps.version.outputs.version }}
        run: npm run build

      - name: Build EXE
        shell: bash
        run: npx electron-builder --win --x64 -c.extraMetadata.main=electron-main.cjs -c.extraMetadata.version=${{ steps.version.outputs.version }}

      - name: Create Release
        uses: softprops/action-gh-release@v2
        if: github.ref == 'refs/heads/main'
        with:
          tag_name: ${{ steps.version.outputs.tag }}
          name: "SafetyLink Core ${{ steps.version.outputs.tag }}"
          files: electron-dist/*.exe
          draft: false
          prerelease: false
