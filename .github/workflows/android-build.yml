name: Android CI Build (DISABLED)

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: This workflow is superseded by build-apk.yml
        required: true

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
    - run: echo "Use build-apk.yml instead"
