<?xml version="1.0" encoding="utf-8"?>
<paths xmlns:android="http://schemas.android.com/apk/res/android">
    <!-- files-path: internal app files (audit logs) -->
    <files-path name="audit_logs" path="." />

    <!-- cache-path: temporary evidence exports -->
    <cache-path name="evidence_cache" path="." />

    <!-- external-path: emergency recordings -->
    <external-path name="emergency_recordings" path="." />
</paths>
