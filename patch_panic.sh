sed -i 's/Log.i(TAG, "Dispatch Result - SMS: " + result.smsOk + ", DB: " + result.dbOk);/Log.i(TAG, "Dispatch Result - SMS: " + result.smsOk + ", DB: " + result.dbOk);\n                        if (!result.dbOk) {\n                            com.aistudio.safetylink.vqnztp.data.OfflineSyncHelper.recordFailedDispatch(\n                                    PanicService.this, incidentId, pendingLat, pendingLng, pendingDescription, pendingOrgId, pendingTriggeredBy\n                            );\n                        }/' android/app/src/main/java/com/aistudio/safetylink/vqnztp/PanicService.java
