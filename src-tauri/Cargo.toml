[package]
name = "safetylink-command"
version = "1.0.0"
description = "SafetyLink Command Deck"
authors = ["TM Media Solutions"]
edition = "2021"

[build-dependencies]
tauri-build = { version = "1.5", features = [] }

[dependencies]
tauri = { version = "1.5", features = ["shell-open"] }
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"
